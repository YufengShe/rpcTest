syntax = "proto3";

package pubSub.rpc.test;
option go_package = "pb/pubSubService";

message String {
  string value = 1;
}

service PubSubService {
  rpc Publish (String) returns (String);
  rpc Subscribe (String) returns (stream String);
}

